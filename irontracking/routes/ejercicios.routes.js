/*
const express = require('express');
const router = express.Router();
const ejerciciosController = require('../controllers/ejercicios.controller');

router.get('/', ejerciciosController.index);
router.get('/new', ejerciciosController.new);
router.post('/', ejerciciosController.create);
router.get('/:id/edit', ejerciciosController.edit);
router.post('/:id', ejerciciosController.update);
router.post('/:id/delete', ejerciciosController.delete);

module.exports = router;
*/